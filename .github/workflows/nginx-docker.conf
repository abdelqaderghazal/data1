# ═══════════════════════════════════════════════════════════════
# Nginx Configuration — Docker Production
# ═══════════════════════════════════════════════════════════════
# ضع هذا الملف بجانب docker-compose.prod.yml
# ═══════════════════════════════════════════════════════════════

upstream dashboard_backend {
    server proxy:3000;
    keepalive 32;
}

server {
    listen 80;
    server_name _;  # يقبل أي نطاق (للتطوير) — اضبطه في الإنتاج

    # Let's Encrypt challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect to HTTPS in production
    # location / {
    #     return 301 https://$host$request_uri;
    # }

    # For development without SSL:
    location / {
        root /var/www/syria-dashboard/public;
        try_files $uri $uri/ /index.html;
        expires 1h;
        add_header Cache-Control "public, immutable";
    }

    location /api/ {
        proxy_pass http://dashboard_backend/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 30s;
    }
}

# Uncomment for HTTPS production:
# server {
#     listen 443 ssl http2;
#     server_name dashboard.yourdomain.com;
#     
#     ssl_certificate /etc/letsencrypt/live/dashboard.yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/dashboard.yourdomain.com/privkey.pem;
#     
#     location / {
#         root /var/www/syria-dashboard/public;
#         try_files $uri $uri/ /index.html;
#     }
#     
#     location /api/ {
#         proxy_pass http://dashboard_backend/;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
